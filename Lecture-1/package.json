{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend works with client server where client requests are fulfilled by server Client requests server and it responds back Two machines working on various operations like authentication, fetching of data etc",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.1.0"
  }
}
